<odoo>

    <record id="inventory_journal_settings_view" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.inventory.journal</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="50"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//form" position="inside">

                <app data-string="Inventory Journal Restrict"
                     string="Inventory Journal Restrict"
                     name="mgs_inventory_journal_restrict">

                    <block title="Inventory Journal Settings"
                           name="mgs_inventory_journal_block">

                        <setting string="Mgs Journal"
                                 id="mgs_journal_ids_setting">

                            <field name="mgs_journal_ids"
                                   widget="many2many_tags"
                                   options="{'no_create': True}"
                                   class="o_light_label"/>

                        </setting>

                    </block>

                </app>

            </xpath>

        </field>
    </record>

</odoo>